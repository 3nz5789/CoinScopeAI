# CoinScopeAI — Claude Code Agent Launch
# Updated: 2026-05-19 | Correct Mac paths verified from live terminal session
# Prerequisite: Claude_Code_Installer completed, logged in as abu3anzeh@gmail.com

# Standard launch (v1 engine repo)
cd ~/Code/CoinScopeAI
claude

# Cowork launch (business docs, scripts, skills)
cd ~/Documents/Claude/Projects/CoinScopeAI
claude

# v2 docs launch (canonical thresholds — read carefully)
cd ~/Documents/Claude/Projects/CoinScopeAI_v2
claude

# macOS Privacy fix (run once if "Folder access denied" appears)
# System Settings → Privacy & Security → Files and Folders
# → Enable Claude access to ~/Code and ~/Documents/Claude
# Then: relaunch Claude Code desktop app

# Important rules
# 1. Do NOT run `claude` from inside an active claude session
#    (you'll see: "You're in ~/Code/CoinScopeAI. I can't launch a new claude instance")
#    Fix: Ctrl+D to exit current session, then run claude again.
#
# 2. Run ONE command per Enter. Never paste multi-line blocks into zsh.
#
# 3. Never cd to ~/Projects/coinscope-ai — that path is RETIRED (2026-05-04).
#    Correct engine path is ~/Code/CoinScopeAI
#
# 4. Validation phase active through ~May 31, 2026.
#    Claude Code may NOT: flip BINANCE_TESTNET=false, bypass risk gates,
#    change order semantics, or declare anything "production-ready".
