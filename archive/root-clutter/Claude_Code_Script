# CoinScopeAI · Claude Code · Prompt Playbook
# Updated: 2026-05-19 | Scoopy voice: phased, explicit assumptions, risk-first, evidence-led.
# Canonical thresholds: MAX_LEVERAGE=10x | MAX_OPEN_POSITIONS=5 | MAX_DD=10% | DAILY_LOSS=5%

# Launch
cd ~/Code/CoinScopeAI
claude

# Phase 1 — Survey the codebase
# > Read all .py files in this directory. For each, give a one-line summary and flag
#   anything that touches the risk-gate, position-size, or regime detector.
#   State your assumptions.

# Phase 2 — Backtest (dry run, testnet assumed)
# > Run python backtest_runner.py --dry-run and show the output.
#   Report drawdown, daily-loss, and max-leverage observed vs limits
#   (MAX_DD=10% / DAILY_LOSS=5% / MAX_LEVERAGE=10x). Flag any breach.

# Phase 3 — Scanner integrity check
# > Check live_scan.py for issues with the score_sentiment function and its call site
#   in _scan_symbol. Don't modify — report findings, confidence, and assumptions first.

# Phase 4 — Live scan (when engine is live post COI-68)
# > Run python live_scan.py and show full output. Report current regime per symbol
#   (Trending / Mean-Reverting / Volatile / Quiet) and which signals would be gated.

# Phase 5 — Risk gate check
# > Read coinscope.env.example and confirm all canonical thresholds are present:
#   MAX_LEVERAGE=10, MAX_OPEN_POSITIONS=5, MAX_DRAWDOWN_PCT=10, MAX_DAILY_LOSS_PCT=5.
#   Report any stale values (=3, =20) found anywhere in the codebase.

# Multi-file edits (Scoopy format)
# State the change, the scope, and the test — in that order.
# > The sentiment_bonus threshold is currently >=4. Change to >=4 AND coverage==5.
#   Scope: sentiment scorer + call sites + docstring.
#   Test: run python3 -m pytest tests/test_ci_smoke.py -v and report pass/fail.

# COI-68 verification (post-VPS patch)
# Run this after VPS is patched and docker restarted:
# > Confirm MAX_OPEN_POSITIONS=5 is live by reading the .env and checking
#   curl http://localhost:8001/risk-gate output. Report the value and flag if =3.

# Guardrails (NEVER skip — validation phase active through May 31, 2026)
# - Binance Testnet ONLY. Never introduce live-order paths (BINANCE_TESTNET=false).
# - No canonical threshold changes without explicit operator approval.
# - No circuit breaker removal, kill-switch bypass, or ML artifact replacement.
# - Every edit must state: assumptions, phase, and rollback plan.
# - Never describe a change as "production-ready".
# - Run python3 scripts/risk_threshold_guardrail.py after any config change.
# - Run python3 scripts/drift_detector.py after any canonical doc edit.

# Key paths (verified 2026-05-19)
# Engine repo:       ~/Code/CoinScopeAI
# Cowork root:       ~/Documents/Claude/Projects/CoinScopeAI
# v2 docs repo:      ~/Documents/Claude/Projects/CoinScopeAI_v2
# VPS engine dir:    /opt/coinscopeai/  (ubuntu@ip-172-31-15-30, DO SGP1)
# RETIRED path:      ~/Projects/coinscope-ai  (DO NOT USE)
