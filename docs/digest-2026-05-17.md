# CoinScopeAI Weekly Digest — 2026-05-17

**Generated:** 2026-05-17 (automated, Sunday 08:00 Asia/Amman)
**Phase:** P0 — 30-day testnet validation cohort (Day ~17 of 30, active, cap 40 users)
**Engine status:** 🔴 Unreachable (localhost:8001 — COI-68 pending operator SSH action)

---

## 1. Decisions (2026-05-10 → 2026-05-17)

Three decisions logged, all dated 2026-05-12:

| Date | Section | Decision |
|---|---|---|
| 2026-05-12 | §15 | **coinscope-mcp-server v0.1 scaffolded** — 15 read-only TypeScript tools, stdio transport, axios + Zod. `npm run build` clean. Not yet registered in any MCP client config. |
| 2026-05-12 | §12 | **Dashboard auth gate landed** — Manus OAuth portal pattern; `OAUTH_PORTAL_URL` env unfilled; middleware short-circuits to authenticated in dev. Provisional. |
| 2026-05-12 | §13 | **Brand assets relocated** — Logos + design assets moved into `CoinScopeAI/16 — Brand & Design/`. Naming convention applied. |

No decisions logged 2026-05-13 through 2026-05-17. Decision log active and append-only.

---

## 2. Code Reviews (2026-05-10 → 2026-05-17)

Two code reviews in window:
- `08-sessions/CODE_REVIEW_2026-05-12_daily.md` (15,357 bytes)
- `08-sessions/CODE_REVIEW_2026-05-10_daily.md` (17,977 bytes)

### Punch List Status: 17 open items

**Top 3 concerns (by severity × age):**

#### 🔴 CRITICAL — Age: 9+ days (first flagged ≤ 2026-05-08)
**`data/binance_websocket.py` — WS_TESTNET_URL is the mainnet URL**
- `testnet=True` silently connects to live Binance, not testnet
- Any process using testnet mode hits real exchange data / real order books
- P0 safety violation: validation phase requires Binance Testnet only (per PCC v2 §8)
- **Status: UNRESOLVED — no Linear ticket, no decision-log deferral entry**
- **Required action:** Create Linear ticket immediately or log explicit deferral with justification

#### 🔴 HIGH — Age: 2 days (flagged 2026-05-12, item N17)
**`core/risk_gate.py` — Daily loss limit uses `initial_capital` not current equity**
- `if self.daily_pnl < -self.initial_capital * self.max_daily_loss_pct:` is incorrect
- Should use current equity; as equity grows, the limit is understated; as it shrinks, overstated
- MAX_DAILY_LOSS_PCT = 5% (canonical, PCC v2 §8 locked) — but the actual gate is miscalibrated
- **Status: NOT FIXED despite being flagged in 2026-05-10 review**

#### 🟡 HIGH — Age: 5+ days
**`signals/backtester.py` — TP1 partial-take is a no-op**
- Reported PnL overstates full-position TP2 results
- Max drawdown understated (equity sampled only on trade close, not intra-trade)
- Backtest results used for signal validation are unreliable until fixed

**Additional findings (2026-05-10 review):**
- N1: `core/tos_gate.py` — sync DB call blocks async FastAPI event loop
- N4: `core/cost_meter.py` — integer rounding zeroes fractional-cent costs
- N7: `core/key_vault.py` — AES-GCM with no AAD (crypto correctness gap)
- N12: `scanners/scalp_scanner.py` — `direction=None` scoring leak

---

## 3. Engine API — Performance & Risk Gate

**Status: 🔴 ALL ENDPOINTS UNREACHABLE**

- `GET http://localhost:8001/performance` — no response (timeout)
- `GET http://localhost:8001/risk-gate` — no response (timeout)
- `GET http://localhost:8001/journal?limit=200` — no response (timeout)

**Root cause:** COI-68 pending. VPS `.env` has not been patched (MAX_OPEN_POSITIONS=3 vs canonical 5). Docker compose has not been restarted. Engine is stale and likely not running.

**Operator action required (SSH, not automatable):**
```
sed -i.bak 's/^MAX_OPEN_POSITIONS=3$/MAX_OPEN_POSITIONS=5/' .env
docker compose down && docker compose up -d --force-recreate
```

Per task rules: no fabricated metrics. Performance section intentionally omitted — engine unreachable.

---

## 4. Drift Detector & Risk Threshold Guardrail

### drift_detector.py
**Exit code: 1 — INDETERMINATE (sandbox I/O error, not token drift)**

The script encountered `OSError: [Errno 35] Resource deadlock avoided` when attempting `path.read_text()` on the mounted filesystem in the digest environment. This is a sandbox filesystem constraint, NOT a canonical drift finding.

**Last confirmed clean pass:** `2026-05-12 00:01 — PASS — 10 files scanned` (source: `incidents/drift-log.md`)

**Gap:** 5 days without a confirmed clean drift scan (2026-05-12 → 2026-05-17).

**Required action:** Operator must run `python3 scripts/drift_detector.py` manually from the Cowork folder on the host machine (not via this sandbox) to get a current clean pass.

### risk_threshold_guardrail.py
**Exit code: 0 — ✓ CLEAN**

All scanned `.py`, `.env*`, `.yml`, `.json` files show no threshold violations. Canonical values confirmed:
- MAX_LEVERAGE = 10x ✓
- MAX_OPEN_POSITIONS = 5 ✓ (in code; VPS .env still has 3 — COI-68)
- MAX_DRAWDOWN_PCT = 10% ✓
- MAX_DAILY_LOSS_PCT = 5% ✓
- POSITION_HEAT_CAP_PCT = 80% ✓
- KELLY_HARD_CAP_PCT = 2% ✓

---

## 5. Open Operator Actions

| Issue | Action | Status |
|---|---|---|
| COI-68 | VPS .env patch (MAX_OPEN_POSITIONS=3→5) + docker restart | 🔴 Urgent — blocks engine, blocks COI-69 |
| COI-69 | Post-restart /config verify + drift clean run | 🔴 Blocked by COI-68 |
| WS_TESTNET_URL bug | Create Linear ticket or log explicit deferral (9+ days, P0 safety) | 🔴 Urgent |
| N17 risk_gate fix | Patch daily loss limit to use current equity | 🔴 High |
| drift_detector | Manual run from host (sandbox cannot complete scan) | 🟡 Medium |

---

## 6. Phase Snapshot

- **P0 validation**: Day ~17 of 30 (target: May 31, 2026 end of validation phase)
- **Testnet only**: All trading MUST be Binance Testnet — `WS_TESTNET_URL` bug violates this
- **Not production-ready**: PCC v2 §8 Capital Cap criteria not yet met; no live trading until §8 gate clears
- **Cohort cap**: 40 users, testnet only; no expansion until post-validation

---

*Digest generated by Scoopy (automated, coinscope-weekly-digest task). Engine API metrics omitted — unreachable. All thresholds quoted from PCC v2 §8 (canonical, locked 2026-05-01). Risk gate figures omitted per anti-fabrication rule.*
