# Kronos research environment — isolated from the main engine venv
# Install with: python3 -m venv .venv-kronos && source .venv-kronos/bin/activate && pip install -r requirements.txt

--index-url https://pypi.org/simple

numpy
pandas
torch>=2.0.0
torchvision
torchaudio
einops==0.8.1
huggingface_hub==0.33.1
matplotlib==3.9.3
pandas==2.2.2
tqdm==4.67.1
safetensors==0.6.2
requests
python-dotenv
